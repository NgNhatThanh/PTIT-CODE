#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
    ifstream inp;
    inp.open("PTIT.in", ios::in);
    ofstream out;
    out.open("PTIT.out", ios::out);
    string s;
    while(getline(inp, s)) out << s << '\n';
} 